{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment": "Optional: paste a RescueGroups API key below for extra coverage (local dev only — do not commit real keys).",
  "RescueGroups": {
    "ApiKey": ""
  }
}
