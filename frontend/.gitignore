# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Intermediate SSR bundle for scripts/prerender.mjs — an artifact of `npm run build`.
.prerender
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Screenshot scripts write to $SCRATCH; unset, node creates a literal "undefined/" dir.
# Ignored because this has now leaked into three commits.
undefined/
